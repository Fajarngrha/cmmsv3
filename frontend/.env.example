# Base URL backend API. Kosongkan jika frontend dan backend di origin yang sama
# (mis. frontend dilayani oleh Express dari folder dist).
# Untuk production ketika frontend di host terpisah, set ke URL backend tanpa trailing slash.
# VITE_API_URL=https://api.example.com
