# Server (opsional; default backend listen di 0.0.0.0:3001)
# PORT=3001
# HOST=0.0.0.0

# PostgreSQL — WAJIB untuk go-live (backend tidak menggunakan mock)
DB_HOST=localhost
DB_PORT=5432
DB_USER=cmms_user
DB_PASSWORD=Acernitro5
DB_NAME=cmms_db

# Atau gunakan satu URL:
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/cmms_db

# Setelah set env, jalankan: psql -d cmms_db -f database/schema-postgres.sql
# Lalu (opsional) data awal: psql -d cmms_db -f database/seed-postgres.sql
